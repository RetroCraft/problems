\documentclass[class=math239,notes,tikz]{agony}
\setsectiontitle{Lecture}
\renewcommand*{\theHchapter}{\theHpart.\arabic{section}}

\title{MATH 239 Fall 2022: Lecture Notes}
\begin{document}
\renewcommand{\contentsname}{MATH 239 Fall 2022:\\{\huge Lecture Notes}}
\thispagestyle{firstpage}
\tableofcontents

Lecture notes taken, unless otherwise specified,
by myself during section 002 of the Fall 2022 offering of MATH 239,
taught by Luke Postle.

Part/chapter titles vaguely follow the official course notes.

\include{notes/enumeration}
\include{notes/graph}

\part{Exam Review}
\setsectiontitle{Tutorial}
\setcounter{section}{11}
\section{(12/05 3:30pm)}

\paragraph{List of things to know}
\begin{itemize}[nosep]
  \item \nameref{thm:hand} and \nameref{lem:face}: $\sum \deg(v) = 2\abs{E} = \sum \deg(f)$
  \item Theorem~\nameref{thm:oddbi}: bipartite if and only if no odd cycles
  \item \nameref{thm:kur}: $K_{3,3}$ or $K_5$ if and only if not planar
  \item \nameref{thm:euler}: $\abs{V} + \abs{E} - \abs{F} = 1 + \abs{V/\sim}$ if planar
  \item \nameref{thm:konig}: if bipartite, size of max matching is size of min cover
  \item \nameref{thm:hall}: given bipartite $(A, B)$, matching saturating all of $A$
        if and only if $\abs{N(D)} \geq D$ for all $D \subseteq A$.
\end{itemize}

\begin{example}[IPT-7-2]
  Let $G$ be $k$-regular with $k \geq 2$.
  Prove that $G$ contains $\binom{k}{2}$ distinct cycles.
\end{example}
\begin{prf}
  Let $P = v_1,\dotsc,v_\ell$ be a longest path. That is:
  \begin{center}
    \tikz\graph{
    v_0[red] --[red] v_1 -- v_2 -- v_3 --[dashed] v_i --[dashed,blue] v_j[x=1] --[dashed] "v_{\ell}"[x=1],
    v_1 --[bend left,blue] v_i, v_1 --[bend right,blue] v_j
    };
  \end{center}
  Note that $v_1$ has $k$ neighbours, all of which are in $P$
  becuase if some \textcolor{red}{$v_0$} existed, then $v_0 + P$ is a longer path.

  For any pair of neighbours $v_i \neq v_j$ with $i < j$,
  we can take the path \textcolor{blue}{$v_1,v_i,v_{i+1},\dotsc,v_j,v_1$},
  giving us at least $\binom{k}{2}$ distinct cycles.
\end{prf}

\begin{example}[IPT-8-4]
  Let $e \in E(G)$. Prove that $e$ is a bridge
  if and only if $e$ is in every spanning tree.
\end{example}
\begin{prf}
  Suppose for a contradiction that $e = uv$ is a bridge
  that is not in some spanning tree $T$.
  Then, $T$ is a spanning tree for $G-e$,
  meaning that there must be a $u,v$-path in $G-e$ by Lemma~\nameref{lem:uniquepath}.
  But then $e$ is not a bridge.

  Conversely, suppose for a contradiction that $e$ is in every spanning tree
  but $e$ is not a bridge.
  Then, $G-e$ is connected and has a spanning tree $T$ by Theorem~\nameref{thm:conspan}.
  But then $T$ is a spanning tree for $G$ that does not contain $e$.
\end{prf}

\begin{example}[IPT-9-1]
  Let $G$ be planar. Prove $G$ has a vertex of degree at most 3 or a face of degree most 3.
\end{example}
\begin{prf}
  Suppose for a contradiction that $\deg(v) > 3$ and $\deg(f) > 3$
  for all $v \in V(G)$ and $f \in F(G)$.
  By the \nameref{thm:hand}, $2\abs{E(G)} = \smashoperator{\sum\limits_{v \in V(G)}}\deg(v) \geq 4\abs{V(G)}$.

  Also, by the \nameref{lem:face}, $2\abs{E(G)} = \smashoperator{\sum\limits_{f \in F(G)}}\deg(v) \geq 4\abs{F(G)}$.

  Now, by \nameref{thm:euler},
  \[
    4\abs{V(G)} - 4\abs{E(G)} + 4\abs{F(G)}
    = (4\abs{V(G)} - 2\abs{E(G)}) + (4\abs{F(G)} - 2\abs{E(G)}) = 4+4k
  \]
  where $k$ is the number of components of $G$.
  But the above inequalities give $4\abs{V(G)} - 2\abs{E(G)} \leq 0$
  and $4\abs{F(G)} - 2\abs{E(G)} \leq 0$
  implying that $k \leq 0$.

  But every graph has at least 1 component. Contradiction.
\end{prf}

\pagebreak
\part{Back Matter}
\renewcommand{\listtheoremname}{List of Named Results}
\phantomsection\addcontentsline{toc}{section}{\listtheoremname}
\listoftheorems[ignoreall,onlynamed={theorem,lemma,corollary}]
\printindex

\end{document}
